{
  "name": "claudegames",
  "version": "1.0.0",
  "description": "Arcade games and a WebGL château, built from scratch in vanilla JavaScript. No engine, no build step.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "build": "node galaga/build.mjs && node gauntlet/build.mjs && node cheverny/build.mjs",
    "check": "npm --prefix galaga run check && npm --prefix gauntlet run check && npm --prefix cheverny run check"
  },
  "license": "MIT"
}
